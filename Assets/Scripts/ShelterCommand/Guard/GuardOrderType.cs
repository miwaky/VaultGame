namespace ShelterCommand
{
    /// <summary>
    /// Direct orders the player can issue to the guard via the telephone.
    /// </summary>
    public enum GuardOrderType
    {
        None,
        MoveToRoom,
        PatrolRoom,
        CancelOrder
    }
}
