namespace ShelterCommand
{
    /// <summary>
    /// Represents the current operational state of the guard.
    /// </summary>
    public enum GuardState
    {
        Idle,
        Moving,
        Guarding,
        Intervening
    }
}
