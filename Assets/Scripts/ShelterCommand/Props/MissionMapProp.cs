using UnityEngine;

namespace ShelterCommand
{
    /// <summary>
    /// Decorative world map poster on the office wall.
    /// No interaction — display only.
    /// </summary>
    public class MissionMapProp : MonoBehaviour
    {
        // Intentionally empty — the WorldMap is purely a visual prop.
    }
}
