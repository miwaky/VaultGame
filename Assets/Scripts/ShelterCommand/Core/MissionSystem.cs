using UnityEngine;

namespace ShelterCommand
{
    /// <summary>
    /// Stub — mission system removed. Component kept so existing scene references don't break.
    /// </summary>
    public class MissionSystem : MonoBehaviour { }
}
